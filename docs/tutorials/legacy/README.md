# Legacy tutorials

This folder contains snapshots of tutorial notebooks before refactors that:

- update visualization keys (e.g. `mode` → `vis_mode`), and
- add safety guards to prevent notebooks from writing to disk or running heavy loops by default.

These files are kept for comparison/rollback during documentation cleanup.
